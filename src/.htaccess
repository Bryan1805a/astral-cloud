RewriteEngine On

# Never rewrite /console/ requests — handled by ProxyPass
RewriteCond %{REQUEST_URI} !^/console/

RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [QSA,L]
