# Proxy /console/ to VM Bridge on the Docker host
# upgrade=websocket tells mod_proxy_wstunnel to handle WebSocket connections
ProxyPass /console/ http://host.docker.internal:10001/console/ upgrade=websocket
ProxyPassReverse /console/ http://host.docker.internal:10001/console/
